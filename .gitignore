node_modules
.vscode-test/**
vsc-extension-quickstart.md

# Build output
/out/
/out-test/
out/test/**
test/**
*.vsix
**/*.map

# Stray compiled JS sitting next to TypeScript sources
src/**/*.js
src/**/*.js.map

# Tooling
.yarnrc

# Lock files
# package-lock.json IS tracked — required by `npm ci` in CI for reproducible builds.
yarn.lock

# Long-form planning docs (keep in workspace, not in repo)
ROADMAP.md